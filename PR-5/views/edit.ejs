<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Movie</title>
    <link rel="stylesheet" href="/style.css">

</head>

<body>

    <div class="app">

        <h1 class="app-title">🎬 Edit Movie</h1>

        <div class="glass-card">

            <form action="/update-movie/<%= movie._id %>" method="post" enctype="multipart/form-data" class="grid">

                <input type="text" name="title" value="<%= movie.title %>" placeholder="Title" required>

                <input type="text" name="genre" value="<%= movie.genre %>" placeholder="Genre" required>

                <input type="text" name="description" value="<%= movie.description %>" placeholder="Description"
                    required>

                <input type="number" name="duration" value="<%= movie.duration %>" placeholder="Duration" required>

                <input type="file" name="movieImg" class="full">

                <button class="primary-btn full">Update Movie</button>

                <a href="/" class="back-btn full">⬅ Back to Dashboard</a>

            </form>

        </div>

    </div>

</body>

</html>