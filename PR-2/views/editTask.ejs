<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Edit Task</title>

    <style>
        * {
            box-sizing: border-box;
            word-wrap: break-word;
            overflow-wrap: break-word;
            font-family: 'Poppins', sans-serif;
            margin: 0;
            padding: 0;
        }

        body {
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: flex-start;
            padding: 40px 0;
        }

        .edit-container {
            width: 420px;
            background: linear-gradient(135deg, #ffffff, #f3f6ff);
            border-radius: 15px;
            padding: 25px 30px;
            box-shadow: 0 12px 30px rgba(0, 0, 0, 0.15);
            transition: transform 0.3s;
        }

        .edit-container:hover {
            transform: translateY(-5px);
        }

        h2 {
            text-align: center;
            margin-bottom: 25px;
            color: #2c3e50;
            letter-spacing: 0.5px;
        }

        /* FORM GROUPS */
        .form-group {
            margin-bottom: 18px;
        }

        label {
            display: block;
            font-size: 14px;
            margin-bottom: 6px;
            color: #444;
            font-weight: 500;
        }

        input,
        select {
            width: 100%;
            padding: 10px 12px;
            border-radius: 8px;
            border: 1px solid #bbb;
            font-size: 14px;
            outline: none;
            transition: border 0.3s, box-shadow 0.3s;
        }

        input:focus,
        select:focus {
            border-color: #3498db;
            box-shadow: 0 0 8px rgba(52, 152, 219, 0.2);
        }

        input[readonly] {
            background: #f2f4ff;
            cursor: not-allowed;
        }

        /* BUTTONS */
        .btn-group {
            display: flex;
            justify-content: space-between;
            margin-top: 25px;
        }

        .btn {
            width: 48%;
            padding: 10px 0;
            border: none;
            border-radius: 8px;
            font-size: 14px;
            font-weight: 600;
            cursor: pointer;
            transition: transform 0.2s, opacity 0.2s;
        }

        .btn-back {
            background: #6c757d;
            color: white;
        }

        .btn-update {
            background: #3498db;
            color: white;
        }

        .btn:hover {
            transform: translateY(-2px);
            opacity: 0.9;
        }

        select {
            background: #fff;
            color: #2c3e50;
        }

        @media (max-width: 480px) {
            .edit-container {
                width: 90%;
                padding: 20px;
            }
        }
    </style>

</head>

<body>

    <div class="edit-container">
        <h2>Edit Task</h2>

        <form action="/update-task" method="post">

            <div class="form-group">
                <label>Task No</label>
                <input type="number" name="no" value="<%= task.no %>" readonly>
            </div>

            <div class="form-group">
                <label>Task Name</label>
                <input type="text" name="name" value="<%= task.tname %>">
            </div>

            <div class="form-group">
                <label>Priority</label>
                <select name="prio">
                    <option value="">--- Select Priority ---</option>
                    <option value="high" <%=task.tprio==='high' ? 'selected' : '' %>>High</option>
                    <option value="medium" <%=task.tprio==='medium' ? 'selected' : '' %>>Medium</option>
                    <option value="low" <%=task.tprio==='low' ? 'selected' : '' %>>Low</option>
                </select>
            </div>

            <div class="btn-group">
                <button class="btn btn-back">Back</button>
                <button type="submit" class="btn btn-update">Update</button>
            </div>

        </form>
    </div>

</body>

</html>